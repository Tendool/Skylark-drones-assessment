# Leave empty (or unset) when the FastAPI backend serves this app's build
# output itself (single-service deploy) -- requests go to the same origin.
# Set to the backend's URL when hosting the frontend separately (e.g. Vercel
# frontend + Render backend).
VITE_API_BASE=
